
## FraudSentinel
> Real-time banking fraud detection with XGBoost + SHAP + LangGraph

## Tech Stack
- XGBoost, SHAP, LangGraph, FastAPI, React, PostgreSQL, Docker

## Setup
1. Install requirements: pip install -r backend/requirements.txt
2. Run API: python -m uvicorn app.main:app --reload
